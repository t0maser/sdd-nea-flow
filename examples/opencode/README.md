# OpenCode example

Este ejemplo muestra una configuracion global minima para registrar el agente
del flujo nea-flow.

1) Copia este archivo a `~/.opencode/opencode.json` (o fusiona el bloque `agent`).
2) En el proyecto, copia la carpeta `skills/` a la raiz.
